name: check
on:
  push:
    branches:
      -main

jobs:
  say-hello:
    runs-on: ubuntu-latest

    step:
      - name: print anson placed
        run: echo "Hello anson , you are rejeted"

        -name: print current folder
         run:ls